# First Implementation Prompt

Copy everything below into the first task opened in this repository.

---

Read `AGENTS.md`, `docs/DIRECTION.md`, `docs/WORKFLOW.md`, `docs/SALVAGE_MANIFEST.md`, and `docs/REFERENCE_INDEX.md`. They are authoritative.

Build Project 01 as a new browser-based 3D game in this repository using Vite, React, TypeScript, React Three Fiber, Drei, Rapier, Zustand, and an eventual Colyseus authoritative server. Do not port the Unity presentation or repair its layouts.

For this first task, complete only Gates 0–2:

1. Create the art bible and fixed scale sheet.
2. Establish the FBX-to-optimized-GLB pipeline and convert the ranger plus representative crop assets.
3. Build one final-quality visual vertical slice containing:
   - an authored warm forest hub clearing;
   - a complete third-person ranger and camera;
   - clearly separated NORTH forage, WEST farm, and EAST mine portal landmarks with wide approaches;
   - one operational portal and a clearly marked HOME return portal;
   - one finished fenced farm plot with grass outside the planting footprint;
   - one visually finished NPC shop interaction;
   - one forage pickup;
   - a compact responsive HUD and icon-only nine-slot hotbar.

Author visible environment composition in Blender and load it as GLB. Do not construct the world at runtime from primitives or hard-coded prop placement. DOM/CSS may be used for responsive UI. Use existing item icons and approved local assets; do not use the rejected generated parchment atlases.

Treat every image under `references/negative/` as a rejection test. Use `references/positive/` as the minimum environment-quality target. Reject mismatched assets, glowing white materials, empty flat terrain, oversized props, obstructive UI, thin typography, excessive text, or anything resembling an asset showroom.

Before expanding beyond the vertical slice, run it and capture screenshots at 1920×1080, 16:10, and 1366×768. Compare those screenshots against the reference folders, report the comparison honestly, and stop for visual approval. Do not implement the full backend or call the game complete during this task.

Keep me updated with short project-level messages: what is being authored now, what visual gate it must pass, and what comes next.

---

